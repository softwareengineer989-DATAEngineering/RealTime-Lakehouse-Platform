environment:
  name: development

paths:
  bronze: /app/data/bronze
  silver: /app/data/silver
  gold: /app/data/gold

checkpoint:
  bronze: /app/checkpoints/bronze
  silver: /app/checkpoints/silver
  gold: /app/checkpoints/gold

kafka:
  bootstrap_servers: localhost:9092